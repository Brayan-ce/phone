import Productos from "@/_pages/admin/Contenido/productos/productos";

export const metadata = {
  title: "Productos | Admin M Importaciones",
  robots: { index: false, follow: false },
};

export default function ProductosPage() {
  return <Productos />;
}
